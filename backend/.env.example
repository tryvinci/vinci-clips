# Backend Environment Variables for Local Development
# This file is for running the backend directly with npm/node (non-Docker)
# Copy this file to backend/.env and fill in your actual values

#############################################
# LOCAL DEVELOPMENT ENVIRONMENT VARIABLES  
#############################################

# Server Configuration
PORT=8080
NODE_ENV=development

# Database Configuration (Local MongoDB)
DB_URL=mongodb://localhost:27017/vinci-clips

# Google Cloud Platform
GCP_BUCKET_NAME=your-gcs-bucket-name
# Path to GCP service account JSON file (relative to backend directory)
GCP_SERVICE_ACCOUNT_PATH=./gcp-service-account.json

# AI Services
GEMINI_API_KEY=your-gemini-api-key

# Redis Configuration (Optional - if running Redis locally)
REDIS_HOST=localhost
REDIS_PORT=6379

# CORS Configuration (for local development)
CORS_ORIGIN=http://localhost:3000


