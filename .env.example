# Environment Variables for Vinci Clips
# This file is for Docker Compose deployment
# Copy this file to .env in the project root and fill in your actual values

#############################################
# DOCKER DEPLOYMENT ENVIRONMENT VARIABLES
#############################################

# Backend Configuration
PORT=8080
NODE_ENV=development

# Database Configuration (Docker)
DB_URL=mongodb://admin:password123@mongodb:27017/vinci-clips?authSource=admin

# Google Cloud Platform
GCP_BUCKET_NAME=your-gcs-bucket-name
# Path to GCP service account JSON file (relative to project root)
GCP_SERVICE_ACCOUNT_PATH=./backend/gcp-service-account.json

# AI Services
GEMINI_API_KEY=your-gemini-api-key

# Redis Configuration (Docker)
REDIS_HOST=redis
REDIS_PORT=6379

# Production Settings (for docker-compose.prod.yml)
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=your-secure-password
CORS_ORIGIN=https://yourdomain.com
NEXT_PUBLIC_API_URL=https://api.yourdomain.com

# SSL Certificate Path (for production nginx)
SSL_CERT_PATH=./nginx/ssl/certificate.crt
SSL_KEY_PATH=./nginx/ssl/private.key